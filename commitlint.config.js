module.exports = {
  extends: ['@commitlint/config-conventional'],
  rules: {
    // 这里可以自定义你的校验规则，例如：
    // 'type-enum': [2, 'always', ['feat', 'fix', 'docs', 'style', 'refactor', 'test', 'chore']],
  },
};
